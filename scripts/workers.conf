# =============================================================================
# workers.conf — Worker node inventory for deploy-workers.sh
#
# Format (one worker per line):
#   <IP or hostname>  <ssh-user>
#
# - ssh-user must have passwordless sudo on the worker node.
# - Lines starting with # and blank lines are ignored.
# =============================================================================

192.168.1.51  star_worker1
192.168.1.52  star_worker2
192.168.1.53  star_worker3
192.168.1.54  star_worker4
