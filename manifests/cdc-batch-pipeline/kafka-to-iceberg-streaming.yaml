# =============================================================================
# kafka-to-iceberg-streaming.yaml
#
# Kubernetes Deployment + ConfigMap for the Kafka→Iceberg CDC streaming consumer.
#
# Three write modes are supported, one Deployment per mode:
#
#   kafka-to-iceberg-standard         — SCD Type 0, MERGE + hard delete
#   kafka-to-iceberg-soft-delete      — MERGE upsert, soft-delete flag on DELETE
#   kafka-to-iceberg-history-tracking — append-only, full row history
#
# Only ONE Deployment should be active at a time (they all consume from the same
# Kafka topics).  Use kubectl scale to enable/disable:
#
#   kubectl scale deployment kafka-to-iceberg-standard -n prod --replicas=1
#   kubectl scale deployment kafka-to-iceberg-soft-delete -n prod --replicas=0
#   kubectl scale deployment kafka-to-iceberg-history-tracking -n prod --replicas=0
#
# Script delivery
# ───────────────
# 05_kafka_to_iceberg_streaming.py is NOT baked into the spark-gluten-velox
# image.  It is delivered via the kafka-to-iceberg-script ConfigMap and
# mounted into every pod at /opt/spark/work-dir/05_kafka_to_iceberg_streaming.py
# (overlaying the image filesystem at that path).
# To update the script: kubectl create configmap kafka-to-iceberg-script
#   --from-file=... --dry-run=client -o yaml | kubectl apply -f -
# Running pods pick up the new version on their next restart.
#
# Auto-restart
# ─────────────
# Kubernetes restartPolicy (built into Deployment): Always.
# The Python script runs an infinite internal retry loop (MAX_RESTART_ATTEMPTS=0).
# A liveness probe on HEALTH_PORT (8080) detects when all Spark streaming queries
# are dead and the process is stuck in a backoff spiral — Kubernetes kills and
# restarts the pod after failureThreshold * periodSeconds (default: 3 × 30s = 90s).
#
# Namespaces: prod
# =============================================================================

# ── ConfigMap: shared streaming config ───────────────────────────────────────

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kafka-to-iceberg-config
  namespace: prod
  labels:
    app: kafka-to-iceberg
    pipeline.type: streaming
data:
  SPARK_USER:              "dave"
  TRIGGER_INTERVAL:        "2 seconds"
  MAX_OFFSETS_PER_TRIGGER: "50000"
  MAX_RESTART_ATTEMPTS:    "0"
  HEALTH_PORT:             "8080"
  RESTART_BACKOFF_BASE_S:  "5"
  RESTART_BACKOFF_MAX_S:   "120"
  DRY_RUN:                 "0"
  ADDR:                    "http://openbao.prod.svc.cluster.local:8200"
  # Peak-hour performance tuning
  # MERGE_PARALLELISM: shuffle partitions for MERGE ops (raise during peak loads)
  MERGE_PARALLELISM:            "8"
  # COALESCE_BEFORE_MERGE: coalesce batch partitions before MERGE to reduce small files
  COALESCE_BEFORE_MERGE:        "4"
  # ADAPTIVE_COALESCE_TARGET: AQE target bytes per post-shuffle partition (64 MB)
  ADAPTIVE_COALESCE_TARGET:     "67108864"
  # TRANSFORM_PIPELINE: comma-separated StarTransform steps applied before write-mode handler.
  # Available: deduplicate, add_processing_time, add_op_label, add_source_tag, mask_pii
  # Leave empty ("") to disable all transforms (default).
  TRANSFORM_PIPELINE:           ""
  # PII_COLUMNS: comma-separated column names to SHA-256 hash when mask_pii is active.
  PII_COLUMNS:                  "email,phone,phone_number,ssn,credit_card"
  # TARGET_NAMESPACE: when set, overrides the Iceberg namespace derived from the Kafka topic
  # for ALL sources (postgres + oracle + mongodb) so every table lands in one namespace.
  # Production: leave empty "" — each topic routes to its own namespace (cache_testing/tpcds).
  # E2E testing: set to "e2e_testing" so all three databases write into e2e_testing.
  #   postgres.cache_testing.customers → postgres.e2e_testing.customers
  #   oracle.cache_testing.CUSTOMERS   → oracle.e2e_testing.customers
  #   mongodb.cache_testing.customers  → mongodb.e2e_testing.customers
  TARGET_NAMESPACE:             ""

# ── Deployment: standard write mode ──────────────────────────────────────────

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-to-iceberg-standard
  namespace: prod
  labels:
    app: kafka-to-iceberg
    pipeline.write-mode: standard
    pipeline.type: streaming
  annotations:
    # Document the restart policy semantics inline for operators
    pipeline.auto-restart: "Deployment restartPolicy=Always + internal retry loop"
    pipeline.write-mode-desc: "SCD Type 0 — MERGE upsert + hard delete"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafka-to-iceberg
      pipeline.write-mode: standard
  template:
    metadata:
      labels:
        app: kafka-to-iceberg
        pipeline.write-mode: standard
    spec:
      # restartPolicy for pods in a Deployment must be Always.
      # K8s restarts the pod automatically on any non-zero exit code.
      restartPolicy: Always
      serviceAccountName: spark-cleanup
      # Allow generous time for the Spark session to flush and checkpoint
      terminationGracePeriodSeconds: 90
      containers:
        - name: kafka-to-iceberg
          image: 192.168.1.50:30500/spark-gluten-velox:3.5.1-13
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -c
            - |
              cd /opt/spark/work-dir && \
              exec python3 05_kafka_to_iceberg_streaming.py
          ports:
            - name: health
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: health
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 5
          env:
            - name: USER
              value: dave
            - name: SPARK_USER
              value: dave
            - name: WRITE_MODE
              value: standard
            - name: TRIGGER_INTERVAL
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: TRIGGER_INTERVAL
            - name: MAX_OFFSETS_PER_TRIGGER
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: MAX_OFFSETS_PER_TRIGGER
            - name: MAX_RESTART_ATTEMPTS
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: MAX_RESTART_ATTEMPTS
            - name: RESTART_BACKOFF_BASE_S
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: RESTART_BACKOFF_BASE_S
            - name: RESTART_BACKOFF_MAX_S
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: RESTART_BACKOFF_MAX_S
            - name: DRY_RUN
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: DRY_RUN
            - name: ADDR
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: ADDR
            - name: MERGE_PARALLELISM
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: MERGE_PARALLELISM
            - name: COALESCE_BEFORE_MERGE
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: COALESCE_BEFORE_MERGE
            - name: ADAPTIVE_COALESCE_TARGET
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: ADAPTIVE_COALESCE_TARGET
            - name: TRANSFORM_PIPELINE
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: TRANSFORM_PIPELINE
            - name: PII_COLUMNS
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: PII_COLUMNS
            - name: TARGET_NAMESPACE
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: TARGET_NAMESPACE
            - name: HEALTH_PORT
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: HEALTH_PORT
          resources:
            requests:
              memory: "4Gi"
              cpu: "2"
            limits:
              memory: "8Gi"
              cpu: "4"
          volumeMounts:
            - name: streaming-script
              mountPath: /opt/spark/work-dir/05_kafka_to_iceberg_streaming.py
              subPath: 05_kafka_to_iceberg_streaming.py
              readOnly: true
            - name: star-transform-script
              mountPath: /opt/spark/work-dir/star_transform.py
              subPath: star_transform.py
              readOnly: true
      volumes:
        - name: streaming-script
          configMap:
            name: kafka-to-iceberg-script
        - name: star-transform-script
          configMap:
            name: star-transform-script

# ── Deployment: soft_delete write mode ───────────────────────────────────────

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-to-iceberg-soft-delete
  namespace: prod
  labels:
    app: kafka-to-iceberg
    pipeline.write-mode: soft-delete
    pipeline.type: streaming
  annotations:
    pipeline.auto-restart: "Deployment restartPolicy=Always + internal retry loop"
    pipeline.write-mode-desc: "MERGE upsert + soft-delete flag (is_deleted, deleted_at)"
spec:
  replicas: 0            # disabled by default — scale to 1 to activate
  selector:
    matchLabels:
      app: kafka-to-iceberg
      pipeline.write-mode: soft-delete
  template:
    metadata:
      labels:
        app: kafka-to-iceberg
        pipeline.write-mode: soft-delete
    spec:
      restartPolicy: Always
      serviceAccountName: spark-cleanup
      terminationGracePeriodSeconds: 90
      containers:
        - name: kafka-to-iceberg
          image: 192.168.1.50:30500/spark-gluten-velox:3.5.1-13
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -c
            - |
              cd /opt/spark/work-dir && \
              exec python3 05_kafka_to_iceberg_streaming.py
          ports:
            - name: health
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: health
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 5
          env:
            - name: USER
              value: dave
            - name: SPARK_USER
              value: dave
            - name: WRITE_MODE
              value: soft_delete
            - name: TRIGGER_INTERVAL
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: TRIGGER_INTERVAL
            - name: MAX_OFFSETS_PER_TRIGGER
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: MAX_OFFSETS_PER_TRIGGER
            - name: MAX_RESTART_ATTEMPTS
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: MAX_RESTART_ATTEMPTS
            - name: RESTART_BACKOFF_BASE_S
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: RESTART_BACKOFF_BASE_S
            - name: RESTART_BACKOFF_MAX_S
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: RESTART_BACKOFF_MAX_S
            - name: DRY_RUN
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: DRY_RUN
            - name: ADDR
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: ADDR
            - name: MERGE_PARALLELISM
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: MERGE_PARALLELISM
            - name: COALESCE_BEFORE_MERGE
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: COALESCE_BEFORE_MERGE
            - name: ADAPTIVE_COALESCE_TARGET
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: ADAPTIVE_COALESCE_TARGET
            - name: TRANSFORM_PIPELINE
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: TRANSFORM_PIPELINE
            - name: PII_COLUMNS
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: PII_COLUMNS
            - name: TARGET_NAMESPACE
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: TARGET_NAMESPACE
            - name: HEALTH_PORT
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: HEALTH_PORT
          resources:
            requests:
              memory: "4Gi"
              cpu: "2"
            limits:
              memory: "8Gi"
              cpu: "4"
          volumeMounts:
            - name: streaming-script
              mountPath: /opt/spark/work-dir/05_kafka_to_iceberg_streaming.py
              subPath: 05_kafka_to_iceberg_streaming.py
              readOnly: true
            - name: star-transform-script
              mountPath: /opt/spark/work-dir/star_transform.py
              subPath: star_transform.py
              readOnly: true
      volumes:
        - name: streaming-script
          configMap:
            name: kafka-to-iceberg-script
        - name: star-transform-script
          configMap:
            name: star-transform-script

# ── Deployment: history_tracking write mode ───────────────────────────────────

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-to-iceberg-history-tracking
  namespace: prod
  labels:
    app: kafka-to-iceberg
    pipeline.write-mode: history-tracking
    pipeline.type: streaming
  annotations:
    pipeline.auto-restart: "Deployment restartPolicy=Always + internal retry loop"
    pipeline.write-mode-desc: "Append-only — every CDC event inserted with _change_type + _change_ts"
spec:
  replicas: 0            # disabled by default — scale to 1 to activate
  selector:
    matchLabels:
      app: kafka-to-iceberg
      pipeline.write-mode: history-tracking
  template:
    metadata:
      labels:
        app: kafka-to-iceberg
        pipeline.write-mode: history-tracking
    spec:
      restartPolicy: Always
      serviceAccountName: spark-cleanup
      terminationGracePeriodSeconds: 90
      containers:
        - name: kafka-to-iceberg
          image: 192.168.1.50:30500/spark-gluten-velox:3.5.1-13
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -c
            - |
              cd /opt/spark/work-dir && \
              exec python3 05_kafka_to_iceberg_streaming.py
          ports:
            - name: health
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: health
            initialDelaySeconds: 60
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 5
          env:
            - name: USER
              value: dave
            - name: SPARK_USER
              value: dave
            - name: WRITE_MODE
              value: history_tracking
            - name: TRIGGER_INTERVAL
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: TRIGGER_INTERVAL
            - name: MAX_OFFSETS_PER_TRIGGER
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: MAX_OFFSETS_PER_TRIGGER
            - name: MAX_RESTART_ATTEMPTS
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: MAX_RESTART_ATTEMPTS
            - name: RESTART_BACKOFF_BASE_S
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: RESTART_BACKOFF_BASE_S
            - name: RESTART_BACKOFF_MAX_S
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: RESTART_BACKOFF_MAX_S
            - name: DRY_RUN
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: DRY_RUN
            - name: ADDR
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: ADDR
            - name: MERGE_PARALLELISM
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: MERGE_PARALLELISM
            - name: COALESCE_BEFORE_MERGE
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: COALESCE_BEFORE_MERGE
            - name: ADAPTIVE_COALESCE_TARGET
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: ADAPTIVE_COALESCE_TARGET
            - name: TRANSFORM_PIPELINE
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: TRANSFORM_PIPELINE
            - name: PII_COLUMNS
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: PII_COLUMNS
            - name: TARGET_NAMESPACE
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: TARGET_NAMESPACE
            - name: HEALTH_PORT
              valueFrom:
                configMapKeyRef:
                  name: kafka-to-iceberg-config
                  key: HEALTH_PORT
          resources:
            requests:
              memory: "4Gi"
              cpu: "2"
            limits:
              memory: "8Gi"
              cpu: "4"
          volumeMounts:
            - name: streaming-script
              mountPath: /opt/spark/work-dir/05_kafka_to_iceberg_streaming.py
              subPath: 05_kafka_to_iceberg_streaming.py
              readOnly: true
            - name: star-transform-script
              mountPath: /opt/spark/work-dir/star_transform.py
              subPath: star_transform.py
              readOnly: true
      volumes:
        - name: streaming-script
          configMap:
            name: kafka-to-iceberg-script
        - name: star-transform-script
          configMap:
            name: star-transform-script
