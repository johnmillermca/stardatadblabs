# =============================================================================
# Apache Ranger — STAGING reference stub
# DNS: postgresql.prod.svc.cluster.local
# Actual deployment via raw manifests
# =============================================================================
image:
  repository: 192.168.1.50:30500/apache-ranger
  tag: "2.4.0"
db:
  host: postgresql.prod.svc.cluster.local
  port: 5432
  name: ranger
  existingSecret: ranger-db-credentials
  userKey: db-user
  passwordKey: db-password
